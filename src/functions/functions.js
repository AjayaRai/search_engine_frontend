import axios from "axios";

export function breakdownUsrQuery(userQuery) {
    /*let analyzeQuery = {
        analyzer: "english",
        text: userQuery
    }

    axios
        .get('/movies_index/_analyze', {
            params: {
                source: JSON.stringify(analyzeQuery),
                source_content_type: 'application/json'
            }
        })
        .then((res) => {
            console.log(res.data);

            let tokensArr = res.data.tokens;

            return tokensArr;
        }).catch((err) => {
        console.error(err);
    })*/
}